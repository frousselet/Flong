# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Xcode project targeting iOS, iPadOS and macOS 26.0, from the multiplatform SwiftUI template.
- String catalog with English as the source language and French translations.
- `swift-format` configuration and a shared `Flong` scheme so builds and formatting are reproducible outside Xcode.
- Issue and pull request templates.
- Product and technical specification in `docs/specification.md`, the reference for every design decision.
- Project conventions in `CLAUDE.md`, and the Google Reader API reference kept in `docs/technical/` for the FreshRSS import.
- MPL-2.0 license, with the source notice carried by every file.
- SQLite storage through GRDB, holding the local data model of the specification behind versioned migrations, with UUIDv7 identifiers that sort by creation time.
- Subscriptions : following a feed by its canonical URL, grouped by the publisher serving it under a name the reader may write over each group, which is also the name and the favicon every article wears wherever it is shown, with renaming, making a source a favourite, a batch path that lands a whole import in one transaction, and removing a source or a whole publisher for good, from a swipe, a long press or the heading's menu, asked for before it happens and taking with it everything that was only there because of that source : its articles whatever the reader did to them, their filings, their place in a story, the marks still waiting for them, the name written over a publisher whose last source it was, the secret it was fetched with, what Spotlight held of it, and its records in the reader's iCloud, which is what carries the removal to their other devices and what a removal arriving from one of them now does here too.
- OPML import, tolerant of the malformed files exporters produce, walking the nesting for every feed at the bottom of it and reporting the lines it could not use, with a screen listing the subscriptions it brought over.
- Private and authenticated feeds : a per-subscriber secret address, HTTP Basic, a bearer token or a fixed header, kept in the keychain and propagated by iCloud Keychain, never in the database, a log, an error message or an export, with a designation per feed of which parameters of an address carry the subscription, kept in the same keychain and travelling the same way, never guessed at since a parameter selects a feed or a filter as often as it names a subscriber, never taken off on the way in since the reader has to be able to open the article, and taken off every address that leaves the device along with the tracking and with anything written in front of the host.
- Sessions on sites the reader subscribes to : signing in happens from the article's own menu, on the site's own page in a web view, only the session it leaves is kept, one sign-in covers every feed of the site, it is sent to that site and no other, and a session the site has ended says so rather than quietly serving teasers.
- Full-text extraction : an article opens on whichever version the reader last chose, carried between their devices by the iCloud key-value store, and asking for the whole one fetches the page behind it once, pulls the article out of the navigation, sidebars, share bars and comments around it, and keeps it beside the feed's version.
- HTML parsing and whitelist sanitization, written without a dependency, dropping scripts, frames, forms and tracking pixels, and resolving every address against the article before vetting its scheme, raising to TLS the ones the view fetches itself since App Transport Security refuses a plain one outright, and leaving a link exactly as the publisher wrote it since the browser opens that one and has its own opinion about upgrading.
- Feed parsing for RSS 0.9x, 1.0 and 2.0, Atom 1.0, JSON Feed 1.1 and h-feed, with feed discovery from a page, tolerance for the malformed feeds publishers serve, and a corpus of them under test.
- Conditional fetching with a token bucket per host, `Retry-After` honoured, backoff with jitter, a body cap enforced while streaming, and a refresh interval derived from each feed's own publication history, a background pass spending its short budget on whichever feeds are most overdue against their own rhythm rather than on whichever were subscribed to first, standing aside in Low Power Mode, never over a network the reader pays for by the megabyte, and never counting against a publisher a request the system would not send at all, which used to quarantine every feed a reader had after two days on a tethered connection.
- Ingestion of fetched feeds into the store, recognizing articles already seen, and the same article arriving through two feeds of one newsroom, which is kept but shown once, refreshing them without touching read or starred state, tracking feed health and quarantining what stays broken, with retention by age and by volume, keeping the address of the picture that stands for each article, stated by the feed or taken from its body, reading the two date spellings that were silently losing an article's own date and leaving it sorted on the day it arrived, saying so when an article carries no date of its own rather than passing off its arrival as a publication, and saying when a publisher went back to an article, after its publication date and never instead of it, since an update is information about an article and not a new place for it in a stream ordered by when things were written.
- The reading interface, on iPhone, iPad and Mac : one column at a time under a system tab bar of four sections, the sources, the subjects and the notices each opened as a panel over the page from a button in the leading corner of every section rather than from a tab or a menu of their own, each carrying its own name save the reader's own, which opens on their face and needs none, and closed by a flick rather than by a button repeating what the gesture offers, a sidebar on Mac, each section keeping its own stack, a stream of everything as it arrives broken by day beside the digest, under a pinned row of bars counting a day an hour at a time, exactly the hours it has, every hour with nothing in it greyed to the height of the shortest bar whether it is past or still to come, running the full width of the column until the list moves under it and narrowing into a glass capsule as it does, following the hour the reader has scrolled to rather than being dragged, carrying no count of what is unread, the sources panel counting instead every article a publisher has given the reader and the views above it counting nothing at all, a read article marked by a tick at the end of its headline rather than dimmed, with an isolated web view following the system appearance, an article presented over the window rather than pushed under the tab bar, growing out of the row that opened it, opening on the picture that row was carrying, run edge to edge under the controls at its own height rather than cropped, and taken out of the body where it came from there, the byline under it in two lines, where it came from and when on the first and who wrote it on the second, every name on the same pill of glass the picture credits wear, the publisher wearing the mark the reader knows them by and each person their own pill, unpicked from the one field a feed has for a whole newsroom and left alone where a comma divides a surname from a forename rather than one person from another, the dates lying on the page under the people rather than on pills of their own, since a pill says somebody and a date is not somebody, each under the glyph that says which it is, one clock for the publication and the same clock turned back on itself for the revision, cut out of the text colour so it follows the appearance and saying in words, to a reader who is not looking at it, what it says in a shape to one who is, each pill a solid fill under the hairline the front page's pictures wear rather than glass, since glass is worth its blur only where there is something behind it to take, washed with the colour its own mark averages to and mixed into the paper rather than laid over it, the transparency divided back out so a logo on nothing gives its colour rather than a pale ghost of it and more of the colour used on a dark page than on paper, read from marks already decoded and never waited on, the box for a picture dropped rather than kept empty where there is none and ready for a face where one is ever found, and put down with a cross since the page it came from never went anywhere, reading and starring, marking a view read, adding a feed from the address of a site, and a page that keeps itself up to date : the window follows the store, so a change from a background refresh, from another device or from an archive reaches it without anyone asking, a burst of them settling into one reload and the article list left alone while an article is open, every automatic trigger going through one entry point that fetches, groups and reads the page back so the front page gains stories from a background pass and from a clock tick and not only from a cold launch, a line at the head of the front page naming what is being brought in while it is being brought in, fetching, grouping, writing the headlines, filing the subjects, indexing, synchronizing, exchanging or tidying, over one bar that crosses the whole pass rather than one that starts again at every stage, its stages weighted by what the store says each will cost and settled before the pass begins so the measure never retreats and never fills before the end, growing into its height under the subjects and shrinking out of it again rather than keeping a place it does not need, wearing the same glass as the pills, held back a quarter of a second so an automatic pass nobody was watching does not flicker, shown on the beat for a pull or a press since the finger is still on the glass, growing out of the top edge over the same curve as the room it opens rather than materializing in it, and kept seven tenths so nothing goes unread, answering to no gesture and gone when there is nothing left to say, with a five-minute clock that asks first and sleeps after, restarted when the reader comes back so returning is itself a tick, a pull on the front page and on no other page saying now rather than soon, drawn by the control UIKit has always had since SwiftUI draws none for a scroll view, tapping under the finger when it takes, fetching every feed and grouping what arrived, asking for the page back once its control has retracted rather than under it, which is what makes the gesture and the command end the same way, and puts the page back at its top when the gesture is over, since the space the control held is not always given back on its own, a gesture always under the thumb inviting the frantic refreshing the page exists to make unnecessary.
- Full-text index over every stored article, kept in step by triggers, insensitive to accents and to endings, rebuildable at any time, with the language of an article detected on the device as it arrives.
- Query language with fields, states, dates, brackets and exclusion, parsed into a tree and compiled to SQL with every value bound, understanding the FreshRSS syntax, and ranking results by relevance when the index can answer the whole query.
- Search from the article list, following what is typed, completing feed and tag names, and held to the targets of the specification by a test over 125,000 articles.
- Marks and collections : starring, writing on or filing an article writes a mark on the article itself, with no second copy anywhere, which is synchronized and which no purge may take, shown as a grid of squares of three natures : the ones every reader has, which are the articles they starred, the ones they wrote on, everything a favourite source has served, everything a favourite author has signed and a directory of every byline the feeds have carried, each cleaned by mechanical rules on the way in of the entities, the whitespace, the address RSS says its own author element holds, the word a publisher writes in front of every credit and the masthead stapled to a name, so that one writer is one row rather than five, cut into the several people one byline names since no format gives a publisher a second author element they use, on the separators that can be nothing else and never on a comma alone, which is one person written backwards as often as two written in a row, so that the article's own page and the list of writers can no longer disagree about how many people wrote a piece, and left alone where cleaning would become a judgement, opened as a searchable list of people rather than of articles, each name wearing the marks of the publishers it appears in, the paper somebody mostly writes for first and four at most since they say where a writer works rather than counting the places and singling a writer out from there or from the menu of an article they signed, which travels as one record per writer whose deletion is what carries the change of mind, the ones they fill article by article, and the ones they describe once and never fill, each carrying between devices only what it is made of, and what the reader chose handed to Spotlight and found in the system search : every article they starred, wrote on or filed, the two hundred and fifty most recent of each favourite source and each favourite author, since a star bounds itself where a favourite does not and one prolific publisher would fill an index the whole device shares, a star never being capped so that saying so is what puts an older article back, and the stories on the front page for exactly as long as they are on it, expiring with the page they belong to and written again whenever it changes rather than whenever the store does, a result opening the article over the window or the story as a page in the digest.
- Synchronization between a reader's devices through their own private CloudKit database, told of another device's changes by a silent push, carrying subscriptions with what the reader singled out and the names they wrote over their publishers, one record per marked article whose deletion is what carries an unmarking, and read states compacted into one record per month, keeping what the server says about each record so a second save is not refused, carrying the whole stream, its recent days as one record per feed and per day rather than one per article and the rest as append-only files in iCloud Documents, one per device and per day, so that no two devices ever write one file, with nothing purged unless the reader asks for their disk back, and iCloud's refusals surfaced rather than retried blindly, with a development command that forgets the change tokens, the tags the server gave each record, the archives already read and everything the model wrote, so that a repair fetches the whole zone back down instead of only sending everything up and writes the headlines and the subjects again rather than finding nothing left to do.
- Long work done in resumable batches and in the background : the feeds an import left unfetched, and a vector for every marked article, computed on the device and shared between them, which lets what the reader kept be searched by meaning rather than by words.
- A full pass when the device is at rest on the mains, shaped after the one Photos runs : every feed rather than those politeness says are due, then the enrichment, the purge, the index and the exchange with iCloud, on a six-hour interval with a floor between two runs and enough jitter that a reader's devices do not wake together to ask the same publishers at the same second, one refresh at a time whichever of the six triggers asked for it, one the reader asked for waiting its turn rather than being refused where they cannot see it, taken by the outermost entry point so that the nightly pass, the clock and the reader's own command can never ask three hundred publishers the same question at the same second, its clock kept on disk and counted from the last pass rather than from whenever something asked, so that it is not pushed out of reach by every launch and every return to the foreground, a pass that runs out of time reported as the ordinary success it is rather than as a failure the scheduler answers by granting less time, and a task that finds no window doing the work against a model of its own rather than silently doing nothing.
- A digest as the main screen : articles grouped into stories, the ones several rooms are covering right now shown as they arrive, a room being a newsroom rather than a feed so a paper with a feed per desk counts once, named and summarized on the device by the system model in the reader's own language whatever language the articles are in and with no date in what it writes since it is shown none, marked as a model's work in front of the line it wrote rather than at the far end of the row, the moment a story was last added to said in the mark a revision wears and set across the whole measure so one edge holds it on every row, and an article's own moment written out to the day and the minute rather than counted back from now, to the rules a desk would apply : every word carrying information, a named actor and a verb of action before a general idea, the words a reader looks for first since a headline is read in a list and often cut short, clarity before cleverness, a ceiling of twelve words that is enforced rather than asked for, never a promise the articles do not keep, and a line beneath that states the angle and answers what the headline had no room for, of who, what, where and why but never when, in one or two sentences under an enforced ceiling, instead of saying the headline again, each one stating the rooms, the count and the shape of the arrival it rests on, and crediting the picture it carries to the publisher whose article it came with, their name in the corner of it on a pill of glass, drawn smaller on a thumbnail than on the lead so it takes the same share of either, and never a byline since what reaches a feed is where a picture came from and never who made it, and the head of the page, the bar over it and the dateline in it washed in three bands read from the lead's own photograph, a story's own page doing the same with the picture of the row that opened it, under the default theme alone and never where the reader has asked for more contrast.
- Subjects across the page, filed by the system model and shown as pills pinned at the head of it and as a rubric above each headline : a catalogue of fifty standard sections seeded at the first launch, in the reader's own language and named in English in the code, which a story is filed into once, one story per call and the answer chosen from the catalogue itself with no way out of choosing, the model writing none of its own since what came of letting it was a drift of near synonyms of the sections that already existed, told apart from a model that could not be asked at all so a moment's failure does not leave a story unfiled for good, an empty vocabulary counting as a question never put rather than as an answer, a section that is renamed taking its stories and the reader's opinion with it, asked with the permissive content-transformation guardrails since the default ones refuse a great deal of ordinary news, greedily so a rebuild does not rewrite a page being read, and which the reader may add their own subjects to.
- A long press on a subject says more of this or less of this, on a score from minus three to three that orders the stories before their weight does, and orders the pills themselves ; a panel lists every subject there is, in the reader's own alphabet, including the ones that have fallen off the page, since a preference nobody can find is a preference nobody can undo.
- A reader's menu in the same corner of every section, wearing the reader's own face : an optional name and picture kept in their own iCloud with nowhere to be sent, and the sites they are signed in to, opened as a panel of its own like the three in the other corner ; the subjects and the notices sit beside the sources instead, since what a reader says in either is said about the page behind it, and the command that asked for a refresh by hand is gone with the menu it was a line in, the page keeping itself up to date through the clock, the full pass at rest and the watcher that follows the store.
- Three themes, chosen under the reader's own face and carried to their other devices : `Défaut` in sans serif on the system's own colours, `Papier` with serif headlines on warm paper, and `Solarized` with headlines in monospace, each stating both appearances and reaching every screen, the rendered article included.
- A danger zone at the foot of the reader's panel, a card of red glass out of the grouped form the settings sit in, deleting everything on this device and in iCloud : the store, the keychain, the preferences, the Spotlight index, the record zone and the shared archive, warning first that another device still holding the subscriptions will put its own copy back.
- The language a summary was asked in travels with it, so changing the language of a device rewrites the page rather than leaving it in a language its reader no longer reads, a summary that comes back in the wrong language is dropped for the article's own headline, and a command at the foot of the sources list asks the model to write it all again.
- The sources list says when there is no model on this device, and which of the three reasons it is, so a page named after its own articles is not mistaken for a broken one.
- The digest is titled with the day's date rather than with its own name, and orders what the reader has said nothing about by when it happened rather than by how many articles it has gathered, so a story that has run all week stops holding the top of a page that has changed overnight.
- An editorial page rather than a control panel : one readable column, serif for what was written and sans for what the application says about it, hairline rules in place of cards, and Liquid Glass left to the system's own navigation bar, where Apple's guidance keeps it.
- Pictures on the page : the first story runs its picture across the column and the others keep one beside them, which one leads being read from the same page the rows are built from so a refresh cannot leave the page with no lead at all, all at three by two, edged with a hairline inside their own corner so a picture that ends in white still stops somewhere, set beside the summary rather than beside the whole story, so a headline keeps the full width of the page above them, decoded at the size they are drawn and cached on disk rather than fetched twice.
- The mark of each publisher beside its name, on every article row, at the head of an article, once at the head of its group in the sources list and in place of the count of rooms on a story, asked for once per publisher rather than once per feed, cropped round with the same hairline inside them so a column of them reads as one column, taken from what one of its feeds states or from the well-known paths a site keeps one at, falling back to a generic mark rather than a hole, and a download cut short asked for again rather than held against the address for the life of the process.
- Notifications, switched on one by one in the reader's own menu and off until they are : the first tells them when a story opens, leading with its own headline, written by the device that shows it since there is no server to send one, asked for at the moment the reader asks for it rather than at first launch, silent while they are looking at the page it would be about, opening that story when it is tapped, and asking of a story exactly what the front page asks of it so that nobody is told about news they then cannot find anywhere.
- Where the reader reads from, under their own face and beside their name : a town completed by MapKit as they type, held to towns and countries by an address filter and resolved into fields rather than read off its own display text, or taken once from the device at the moment they press the button and never before, kept as a name and the ISO code of its country and never as a coordinate, carried between their devices by the key-value store with the rest of what they chose, and taken back by the command that deletes everything.
